import axios from 'axios';
// import { getToken } from './auth';

// const apiSet = axios.create({
//   baseURL: process.env.REACT_APP_SET_BASE_URL
// });

// apiSet.interceptors.request.use(config => {
//   const token = getToken();
//   if (token) {
//     config.headers.Authorization = `Bearer ${token}`;
//   }
//   return config;
// });

// const apiIsis = axios.create({
//   baseURL: process.env.REACT_APP_ISIS_BASE_URL
// });

// apiIsis.interceptors.request.use(config => {
//   const token = getToken();
//   if (token) {
//     config.headers.Authorization = `Bearer ${token}`;
//   }
//   return config;
// });

// const apiOsiris = axios.create({
//   baseURL: process.env.REACT_APP_OSIRIS_BASE_URL
// });

// apiOsiris.interceptors.request.use(config => {
//   const token = getToken();
//   if (token) {
//     config.headers.Authorization = `Bearer ${token}`;
//   }
//   return config;
// });

// export { apiSet, apiIsis, apiOsiris };
